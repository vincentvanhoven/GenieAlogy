name: Run Go tests

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    name: "Run Go tests"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: 1.24.0
      - name: Build frontend
        run: cd frontend && npm run dev && cd ..
      - name: Run tests
        run: go test -v ./...